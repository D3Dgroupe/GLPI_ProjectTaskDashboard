{% for warning in warnings %}
<div class="alert alert-warning py-2" role="alert">
  <i class="ti ti-alert-triangle me-1"></i>{{ warning }}. Le tableau de bord reste disponible sans cette colonne.
</div>
{% endfor %}
